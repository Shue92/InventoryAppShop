<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>
<% @users.each do |user| %>
    <li>
     
      <%= link_to user.name, user %>
     
       |  <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
    
    </li>
  <% end %>

<%= will_paginate @users %>